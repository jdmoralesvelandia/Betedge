VITE_API_BASE_URL=http://localhost:8080
# Not a secret - Google's own docs say the OAuth client id is safe in client-side code. Same value
# the backend uses as its required `aud` (see application.yml's google.oauth.client-id).
VITE_GOOGLE_CLIENT_ID=848697196247-cls6qjq919e5b82tjsvs3vjtnm4lgl10.apps.googleusercontent.com
