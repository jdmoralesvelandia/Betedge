package com.betedge.auth;

/** The ID token failed verification (signature, audience, expiration) or its email isn't verified. */
public class InvalidGoogleTokenException extends RuntimeException {
}
